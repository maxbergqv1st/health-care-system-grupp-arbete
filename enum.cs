namespace App;

public enum UStatus
{
    Admin,
    Patient,
    Doctor,
}
public enum Uactivity
{
    activ,
    inactive,
    offline,

}
public enum Startmenu
{
    login = 1,
    register = 2,
    exit = 3,
}
public enum Mainmenu
{
    logout = 1,
    settings = 2,
    profile = 3,
    exit = 4,
}
public enum Region
{
    North, // = 1,
    West,
    East,
    South,
    Everything
}
